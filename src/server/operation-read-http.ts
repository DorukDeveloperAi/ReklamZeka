import { NextResponse } from "next/server";
import type { OperationReadService } from "@/application/operation-read-service";
const headers = Object.freeze({ "Cache-Control": "private, no-store", "X-Content-Type-Options": "nosniff", "X-ReklamZeka-Access-Mode": "read-only", "X-ReklamZeka-Action-Authority": "none", "X-ReklamZeka-Meta-Write": "disabled" });
export const operationReadUnavailable = () => NextResponse.json({ error: { code: "source_unavailable", message: "Operasyon kaynağı kullanılamıyor." } }, { status: 503, headers });
export function createOperationReadHttpHandler(input: Readonly<{ service: OperationReadService; workspaceId(request: Request): Promise<string | null> }>) { return async (request: Request) => { try { const url = new URL(request.url); if (request.method !== "GET" || !request.headers.get("cookie") || request.headers.has("authorization") || request.headers.get("sec-fetch-site") !== "same-origin" || request.headers.get("x-reklamzeka-intent") !== "operation-read" || [...url.searchParams.keys()].some((key) => !["period", "start", "end", "slice", "limit", "cursor"].includes(key))) throw new Error("input"); const workspaceId = await input.workspaceId(request); if (!workspaceId) return NextResponse.json({ error: { code: "local_session_required" } }, { status: 401, headers }); const requestInput = Object.fromEntries(url.searchParams) as Parameters<OperationReadService["read"]>[1]; return NextResponse.json(await input.service.read(workspaceId, requestInput), { headers }); } catch { return NextResponse.json({ error: { code: "invalid_input" } }, { status: 400, headers }); } }; }
