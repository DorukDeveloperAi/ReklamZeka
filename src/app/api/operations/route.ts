import { drizzle } from "drizzle-orm/node-postgres"; import { Pool } from "pg"; import * as schema from "@/db/schema"; import { createLocalOperationReadHandler, localDecisionRoomConfig } from "@/server/local-operation-read-runtime"; import { operationReadUnavailable } from "@/server/operation-read-http";
export const dynamic="force-dynamic"; export const runtime="nodejs"; let handler: ReturnType<typeof createLocalOperationReadHandler>|null=null;
export function GET(request:Request){try{if(!handler){const config=localDecisionRoomConfig(process.env as never);if(!config||!process.env.DATABASE_URL)return operationReadUnavailable();handler=createLocalOperationReadHandler({database:drizzle(new Pool({connectionString:process.env.DATABASE_URL,max:2}),{schema}),config});}return handler(request)}catch{return operationReadUnavailable()}}
