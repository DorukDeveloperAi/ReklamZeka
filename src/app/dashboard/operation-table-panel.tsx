"use client";

import { Fragment, useEffect, useRef, useState } from "react";
import { LocalSessionConnector } from "./local-session-connector";
import styles from "./operation-table-panel.module.css";

type OperationRow = Readonly<{ market: "yerli" | "yabanci" | "unknown"; accountRef: string; organizationCampaignRef: string | null; organizationCampaignName: string; campaignRef: string; adSetRef: string | null; accountName: string; campaignName: string; adSetName: string | null; currentBudgetMinor: number | null; budgetOwner: "campaign" | "ad_set" | "unknown"; budgetOwnerRef: string | null; spendMinor: number | null; primaryResultState: "bound" | "unbound"; primaryResult: number | null; primaryResultCostMinor: number | null; sourceState: "ready" | "partial" | "empty" | "unavailable"; missingDays: readonly string[]; reasonCodes: readonly string[] }>;
export type OperationTableProjection = Readonly<{ version: "operation-read/1.0.0"; period: Readonly<{ startDate: string; endDate: string }>; state: "ready" | "partial" | "empty" | "unavailable"; rows: readonly OperationRow[]; budgetOwners: readonly Readonly<{ ref: string; currentBudgetMinor: number }>[]; authority: Readonly<{ canWriteMeta: false; canExecute: false; canApprove: false }>; nextCursor: string | null }>;
type ReadState = Readonly<{ status: "loading" | "ready" | "empty" | "session_required" | "forbidden" | "conflict" | "unavailable"; projection?: OperationTableProjection; message?: string }>;
type Period = "today" | "7d" | "30d";
const ref = /^[a-z][a-z0-9_]{1,64}_[A-Za-z0-9_.:-]{1,190}$/;
const date = /^\d{4}-\d{2}-\d{2}$/;
const safeText = (value: unknown, max = 320) => typeof value === "string" && value.length > 0 && value.length <= max && !/[\u0000-\u001f\u007f]/.test(value);
const safeMoney = (value: unknown): value is number | null => value === null || typeof value === "number" && Number.isSafeInteger(value) && value >= 0;
const calendarDate = (value: string) => { if (!date.test(value)) return false; const [year, month, day] = value.split("-").map(Number); const parsed = new Date(Date.UTC(year!, month! - 1, day!)); return parsed.getUTCFullYear() === year && parsed.getUTCMonth() === month! - 1 && parsed.getUTCDate() === day; };

/** Rejects malformed rows rather than displaying an untrusted fallback portfolio. */
export function parseOperationTableProjection(value: unknown): OperationTableProjection | null {
  if (!value || typeof value !== "object" || Array.isArray(value)) return null;
  const payload = value as Record<string, unknown>;
  if (Object.keys(payload).some((key) => !["version", "period", "state", "rows", "budgetOwners", "authority", "nextCursor"].includes(key)) || payload.version !== "operation-read/1.0.0" || !["ready", "partial", "empty", "unavailable"].includes(String(payload.state)) || !Array.isArray(payload.rows) || !Array.isArray(payload.budgetOwners) || !(payload.nextCursor === null || typeof payload.nextCursor === "string" && /^operation_cursor_[A-Za-z0-9_-]{1,512}$/.test(payload.nextCursor))) return null;
  const period = payload.period; const authority = payload.authority;
  if (!period || typeof period !== "object" || Array.isArray(period) || Object.keys(period).length !== 2 || !calendarDate(String((period as Record<string, unknown>).startDate)) || !calendarDate(String((period as Record<string, unknown>).endDate)) || String((period as Record<string, unknown>).startDate) > String((period as Record<string, unknown>).endDate) || !authority || typeof authority !== "object" || Array.isArray(authority) || Object.keys(authority).length !== 3 || (authority as Record<string, unknown>).canWriteMeta !== false || (authority as Record<string, unknown>).canExecute !== false || (authority as Record<string, unknown>).canApprove !== false) return null;
  const rows: OperationRow[] = [];
  for (const candidate of payload.rows) { if (!candidate || typeof candidate !== "object" || Array.isArray(candidate)) return null; const row = candidate as Record<string, unknown>;
    const allowed = ["market", "accountRef", "organizationCampaignRef", "organizationCampaignName", "campaignRef", "adSetRef", "accountName", "campaignName", "adSetName", "currentBudgetMinor", "budgetOwner", "budgetOwnerRef", "spendMinor", "primaryResultState", "primaryResult", "primaryResultCostMinor", "sourceState", "missingDays", "reasonCodes"];
    if (Object.keys(row).some((key) => !allowed.includes(key)) || !["yerli", "yabanci", "unknown"].includes(String(row.market)) || ![row.accountRef, row.campaignRef].every((item) => typeof item === "string" && ref.test(item)) || !(row.organizationCampaignRef === null || typeof row.organizationCampaignRef === "string" && ref.test(row.organizationCampaignRef)) || !(row.adSetRef === null || typeof row.adSetRef === "string" && ref.test(row.adSetRef)) || ![row.accountName, row.organizationCampaignName, row.campaignName].every((item) => safeText(item)) || !(row.adSetName === null || safeText(row.adSetName)) || !safeMoney(row.currentBudgetMinor) || !safeMoney(row.spendMinor) || !safeMoney(row.primaryResultCostMinor) || !["campaign", "ad_set", "unknown"].includes(String(row.budgetOwner)) || !(row.budgetOwnerRef === null || typeof row.budgetOwnerRef === "string" && ref.test(row.budgetOwnerRef)) || !["bound", "unbound"].includes(String(row.primaryResultState)) || !(row.primaryResult === null || typeof row.primaryResult === "number" && Number.isFinite(row.primaryResult) && row.primaryResult >= 0) || row.primaryResultState === "unbound" && (row.primaryResult !== null || row.primaryResultCostMinor !== null) || !["ready", "partial", "empty", "unavailable"].includes(String(row.sourceState)) || !Array.isArray(row.missingDays) || !Array.isArray(row.reasonCodes) || row.missingDays.some((item) => typeof item !== "string" || !calendarDate(item)) || row.reasonCodes.some((item) => typeof item !== "string" || !/^[a-z][a-z0-9_:-]{0,80}$/.test(item))) return null;
    rows.push(Object.freeze(row as unknown as OperationRow));
  }
  if ((payload.state === "empty" || payload.state === "unavailable") && rows.length) return null;
  const owners: { ref: string; currentBudgetMinor: number }[] = [];
  for (const candidate of payload.budgetOwners) { if (!candidate || typeof candidate !== "object" || Array.isArray(candidate) || Object.keys(candidate).length !== 2) return null; const owner = candidate as Record<string, unknown>; if (typeof owner.ref !== "string" || !ref.test(owner.ref) || typeof owner.currentBudgetMinor !== "number" || !Number.isSafeInteger(owner.currentBudgetMinor) || owner.currentBudgetMinor < 0) return null; owners.push(Object.freeze({ ref: owner.ref, currentBudgetMinor: owner.currentBudgetMinor })); }
  const periodRecord = period as Record<string, unknown>;
  return Object.freeze({ version: "operation-read/1.0.0", period: Object.freeze({ startDate: periodRecord.startDate as string, endDate: periodRecord.endDate as string }), state: payload.state as OperationTableProjection["state"], rows: Object.freeze(rows), budgetOwners: Object.freeze(owners), authority: Object.freeze({ canWriteMeta: false, canExecute: false, canApprove: false }), nextCursor: payload.nextCursor as string | null });
}

export function operationRowIdentity(row: OperationRow): string { return `${row.accountRef}\u0000${row.campaignRef}\u0000${row.adSetRef ?? "campaign"}`; }
export function operationAppendRows(existing: readonly OperationRow[], incoming: readonly OperationRow[]): readonly OperationRow[] { const seen = new Set(existing.map(operationRowIdentity)); return Object.freeze([...existing, ...incoming.filter((row) => !seen.has(operationRowIdentity(row)) && (seen.add(operationRowIdentity(row)), true))]); }
export class OperationPaginationGate { private inFlight = false; private readonly consumed = new Set<string>(); claim(cursor: string | null): boolean { if (this.inFlight || cursor === null || this.consumed.has(cursor)) return false; this.inFlight = true; this.consumed.add(cursor); return true; } complete() { this.inFlight = false; } reset() { this.inFlight = false; this.consumed.clear(); } }
export function operationProjectionReadState(projection: OperationTableProjection): ReadState { if (projection.state === "unavailable") return Object.freeze({ status: "unavailable", message: "Operasyon kaynağı kullanılamıyor; kanonik veri gösterilmiyor." }); return Object.freeze({ status: projection.state === "empty" ? "empty" : "ready", projection }); }
export function operationHierarchy(rows: readonly OperationRow[]) { const grouped = new Map<string, { key: string; label: string; campaigns: Map<string, { campaignRef: string; campaignName: string; summary: OperationRow | null; adSets: OperationRow[] }> }>();
  for (const row of rows) { const key = `${row.market}\u0000${row.accountRef}\u0000${row.organizationCampaignRef ?? "unassigned"}`; const group = grouped.get(key) ?? { key, label: `${row.market === "unknown" ? "Pazar bilinmiyor" : row.market === "yerli" ? "Yerli" : "Yabancı"} · ${row.accountName} · ${row.organizationCampaignName}`, campaigns: new Map() }; const campaign = group.campaigns.get(row.campaignRef) ?? { campaignRef: row.campaignRef, campaignName: row.campaignName, summary: null, adSets: [] }; if (row.adSetRef) campaign.adSets.push(row); else campaign.summary = row; group.campaigns.set(row.campaignRef, campaign); grouped.set(key, group); }
  return [...grouped.values()];
}

/** The read contract does not currently carry a verified workspace currency.
 * Do not turn a minor-unit number into an invented TRY amount. */
function money(value: number | null) {
  return value === null ? "Veri yok" : "Para birimi doğrulanmadı";
}
function sourceDetail(row: OperationRow) { const missing = row.missingDays.length ? `Eksik gün: ${row.missingDays.join(", ")}` : null; const reasons = row.reasonCodes.length ? `Neden: ${row.reasonCodes.join(", ")}` : null; return [missing, reasons].filter(Boolean).join(" · ") || "Kaynak kapsamı tamam"; }
function metric(row: OperationRow) { return row.primaryResultState === "unbound" ? "Ana sonuç seçilmedi" : row.primaryResult === null ? "Sonuç verisi yok" : new Intl.NumberFormat("tr-TR", { maximumFractionDigits: 2 }).format(row.primaryResult); }
function statusLabel(status: ReadState["status"]) { return ({ loading: "Kampanya kaynağı okunuyor…", session_required: "Operasyon için yerel oturum gerekli.", forbidden: "Bu çalışma alanında Operasyon verisine erişim yok.", conflict: "Operasyon kaynağı güncel bağlamla çakışıyor.", unavailable: "Operasyon kaynağı kullanılamıyor.", empty: "Seçili dönemde gösterilebilir operasyon satırı yok.", ready: "" })[status]; }

export function OperationTableSurface({ state, period, onPeriod, onLoadMore, onConnect, loadingMore = false }: Readonly<{ state: ReadState; period: Period; onPeriod(period: Period): void; onLoadMore(): void; onConnect(): Promise<boolean>; loadingMore?: boolean }>) {
  const [expanded, setExpanded] = useState<ReadonlySet<string>>(new Set()); const groups = state.projection ? operationHierarchy(state.projection.rows) : [];
  const toggle = (campaignRef: string) => setExpanded((current) => { const next = new Set(current); next.has(campaignRef) ? next.delete(campaignRef) : next.add(campaignRef); return next; });
  return <section className={styles.operationTable} aria-label="Operasyon ana tablosu"><header><span className="kicker">OPERASYON · KANONİK OKUMA</span><h2>Portföy işletim tablosu</h2><p>Hiyerarşi, dönem performansı ve veri eksikleri aynı kanonik okumadan gelir. Bu ekran Meta’ya yazmaz.</p></header><details className={styles.help}><summary>Bu tablo nasıl okunur?</summary><p>Satırlar pazar → reklam hesabı → Kurum Kampanyası → Meta kampanyası → reklam seti sırasındadır. Para birimi kanonik yanıtta doğrulanana kadar parasal tutarlar biçimlendirilmez; eksik veri sıfır sayılmaz.</p></details><div className={styles.toolbar} aria-label="Tarih aralığı">{([ ["today", "Bugün"], ["7d", "Son 7 gün"], ["30d", "Son 30 gün"] ] as const).map(([id, label]) => <button key={id} type="button" aria-pressed={period === id} onClick={() => onPeriod(id)}>{label}</button>)}</div>{state.projection?.state === "partial" ? <p className={styles.sourceNote} role="status">Kaynak kısmi: eksik tarih ve nedenler satırda görünür; eksik veri sıfır sayılmaz.</p> : null}{state.status !== "ready" && state.status !== "empty" ? <div role={state.status === "loading" ? "status" : "alert"}><p>{state.message ?? statusLabel(state.status)}</p><p>Kaynak doğrulanana kadar ekran örnek içerikle doldurulmaz.</p>{state.status === "session_required" ? <LocalSessionConnector title="Operasyon oturumunu bağlayın" onVerify={onConnect} /> : null}</div> : null}{state.status === "empty" ? <p role="status">{statusLabel("empty")}</p> : null}{state.status === "ready" && state.projection ? <><p className={styles.sourceNote}>{state.projection.period.startDate} – {state.projection.period.endDate} · Salt-okunur · Meta yazma yetkisi yok</p><div className={styles.scroll}><table className={styles.table}><caption>Hiyerarşik operasyon tablosu; kampanyaları açarak reklam seti satırlarını görün.</caption><thead><tr><th>Kapsam / ad</th><th>Bütçe sahibi</th><th>Güncel bütçe</th><th>Dönem harcaması</th><th>Ana sonuç</th><th>Sonuç maliyeti</th><th>Veri durumu</th></tr></thead><tbody>{groups.map((group) => <Fragment key={group.key}><tr className={styles.group}><td colSpan={7}>{group.label}</td></tr>{[...group.campaigns.values()].map((campaign) => <Fragment key={campaign.campaignRef}>{campaign.summary ? <OperationDataRow row={campaign.summary} expanded={expanded.has(campaign.campaignRef)} hasChildren={campaign.adSets.length > 0} onToggle={() => toggle(campaign.campaignRef)} /> : <CampaignContextRow campaignName={campaign.campaignName} />}{campaign.summary && !expanded.has(campaign.campaignRef) ? null : campaign.adSets.map((row) => <OperationDataRow key={operationRowIdentity(row)} row={row} child />)}</Fragment>)}</Fragment>)}</tbody></table></div><div className={styles.mobileCards}>{state.projection.rows.map((row) => <article className={styles.card} key={operationRowIdentity(row)}><h3>{row.adSetRef ? `Reklam seti · ${row.adSetName ?? "Adı bilinmiyor"}` : row.campaignName}</h3><p>{row.market === "yerli" ? "Yerli" : row.market === "yabanci" ? "Yabancı" : "Pazar bilinmiyor"} · {row.accountName} · {row.organizationCampaignName}{row.adSetRef ? ` · Kampanya: ${row.campaignName}` : ""}</p><dl><div><dt>Bütçe</dt><dd>{money(row.currentBudgetMinor)}</dd></div><div><dt>Harcama</dt><dd>{money(row.spendMinor)}</dd></div><div><dt>Ana sonuç</dt><dd>{metric(row)}</dd></div><div><dt>Veri</dt><dd>{row.sourceState === "ready" ? "Hazır" : row.sourceState === "partial" ? "Kısmi" : row.sourceState === "empty" ? "Boş" : "Kullanılamıyor"}</dd></div></dl><small>{sourceDetail(row)}</small></article>)}</div>{state.projection.nextCursor ? <button className={styles.loadMore} type="button" disabled={loadingMore} aria-busy={loadingMore} onClick={onLoadMore}>{loadingMore ? "Yükleniyor…" : "Daha fazlasını yükle"}</button> : null}</> : null}</section>;
}

function CampaignContextRow({ campaignName }: Readonly<{ campaignName: string }>) { return <tr><td><strong>{campaignName}</strong><small>Yalnız reklam seti kapsamı; kampanya performansı veya bütçesi bu sayfada verilmedi.</small></td><td colSpan={6}>Kampanya bağlamı</td></tr>; }

function OperationDataRow({ row, child = false, expanded, hasChildren, onToggle }: Readonly<{ row: OperationRow; child?: boolean; expanded?: boolean; hasChildren?: boolean; onToggle?: () => void }>) { return <tr className={child ? styles.adSet : undefined}><td>{hasChildren ? <button className={styles.expand} type="button" aria-expanded={expanded} aria-label={`${row.campaignName} reklam setlerini ${expanded ? "daralt" : "aç"}`} onClick={onToggle}>{expanded ? "−" : "+"}</button> : null}<strong>{child ? `Reklam seti · ${row.adSetName ?? "Adı bilinmiyor"}` : row.campaignName}</strong><small>{child ? row.campaignName : `${row.accountName} · ${row.organizationCampaignName}`}</small></td><td>{row.budgetOwner === "unknown" ? "Bütçe sahibi bilinmiyor" : row.budgetOwner === "campaign" ? "Kampanya bütçesi" : "Reklam seti bütçesi"}</td><td>{money(row.currentBudgetMinor)}</td><td>{money(row.spendMinor)}</td><td>{metric(row)}</td><td>{row.primaryResultState === "unbound" ? "—" : money(row.primaryResultCostMinor)}</td><td><span className={styles.state} data-state={row.sourceState}>{row.sourceState === "ready" ? "Hazır" : row.sourceState === "partial" ? "Kısmi" : row.sourceState === "empty" ? "Boş" : "Kullanılamıyor"}</span><small>{sourceDetail(row)}</small></td></tr>; }

export function OperationTablePanel({ onConnect }: Readonly<{ onConnect(): Promise<boolean> }>) { const [period, setPeriod] = useState<Period>("7d"); const [state, setState] = useState<ReadState>({ status: "loading" }); const [cursor, setCursor] = useState<string | null>(null); const [loadingMore, setLoadingMore] = useState(false); const rowsRef = useRef<readonly OperationRow[]>([]); const gateRef = useRef(new OperationPaginationGate()); const controllerRef = useRef<AbortController | null>(null); const requestRef = useRef(0);
  const load = async (nextCursor: string | null, append: boolean, requestedPeriod: Period) => { if (append && !gateRef.current.claim(nextCursor)) return; if (!append) { controllerRef.current?.abort(); controllerRef.current = new AbortController(); rowsRef.current = []; setCursor(null); setState({ status: "loading" }); } else setLoadingMore(true); const request = ++requestRef.current; try { const params = new URLSearchParams({ period: requestedPeriod, limit: "100" }); if (nextCursor) params.set("cursor", nextCursor); const response = await fetch(`/api/operations?${params}`, { cache: "no-store", credentials: "same-origin", signal: controllerRef.current?.signal, headers: { "X-ReklamZeka-Intent": "operation-read" } }); const payload: unknown = await response.json().catch(() => null); if (request !== requestRef.current) return; if (!response.ok) { const status = response.status === 401 ? "session_required" : response.status === 403 ? "forbidden" : response.status === 409 ? "conflict" : "unavailable"; setState({ status, message: statusLabel(status) }); return; } const parsed = parseOperationTableProjection(payload); if (!parsed) { setState({ status: "unavailable", message: "Operasyon kaynağı beklenen kanonik sözleşmeyle doğrulanamadı; veri gösterilmiyor." }); return; } if (parsed.state === "unavailable") { rowsRef.current = []; setCursor(null); setState(operationProjectionReadState(parsed)); return; } const rows = append ? operationAppendRows(rowsRef.current, parsed.rows) : parsed.rows; rowsRef.current = rows; const projection = Object.freeze({ ...parsed, rows }); setCursor(projection.nextCursor); setState(operationProjectionReadState(projection)); } catch (error) { if (request === requestRef.current && !(error instanceof DOMException && error.name === "AbortError")) setState({ status: "unavailable", message: "Operasyon kaynağına ulaşılamadı; veri gösterilmiyor." }); } finally { if (append) { gateRef.current.complete(); if (request === requestRef.current) setLoadingMore(false); } } };
  useEffect(() => { gateRef.current.reset(); void load(null, false, period); return () => controllerRef.current?.abort(); }, [period]);
  const onPeriod = (next: Period) => { if (next !== period) setPeriod(next); };
  return <OperationTableSurface state={state} period={period} loadingMore={loadingMore} onPeriod={onPeriod} onLoadMore={() => cursor && void load(cursor, true, period)} onConnect={onConnect} />;
}
